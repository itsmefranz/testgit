this is my first git file, wag mo gagalawin!
# to stage a file
git add <filename>
# to commit a file
git commit -m "message"
# to list status
git status

# to add remote origin
git remote add origin https://github.com/itsmefranz/testgit2.git

#to push committed files from local to remote
git push -u origin <branchname>